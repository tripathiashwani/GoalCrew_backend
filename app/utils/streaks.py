from typing import Any
from uuid import UUID


async def calculate_streak(user_id: UUID) -> int:
    return 0


async def update_streak_on_reflection(user_id: UUID, reflection_id: str) -> None:
    pass
